#include<stdio.h>
int main()
{
    float a,b,max;
    printf("enter any number :");
    scanf("%d %d",&a,&b);
    if(a>b)
    max=a;
    else
    max=b;
    printf("maximum = %d",max);

    }
